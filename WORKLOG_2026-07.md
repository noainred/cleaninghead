# BrainBloom 작업 기록 — 2026년 7월 세션

> 이 문서는 한 번의 긴 작업 세션에서 진행한 **모든 변경 사항**을 준호님이 나중에 다시 봐도 이해하기 쉽도록 정리한 기록입니다.
> 버전 순서대로 "무엇을 · 왜 · 어떻게" 를 풀어 썼습니다. (기술 상세는 각 항목의 접힌 부분에)

- **세션 범위**: `v3.83.0` → **`v3.95.3`** (총 **20여 개 릴리스**)
- **배포처**: [www.redmir.net](https://www.redmir.net) (GitHub Pages, `main` 브랜치 자동 배포)
- **개발 방식**: 준호님 요청 → 구현 → Babel 컴파일 검증 → 스냅샷/CHANGELOG 갱신 → 커밋 → PR → `main` 머지(=배포)
- **품질 관리**: 큰 변경은 **다중 에이전트 병렬 리뷰**(적대적 코드리뷰·보안 감사)로 검증 후 배포

---

## 📋 한눈에 보기 — 이번 세션에 새로 생긴 것

| 분류 | 핵심 결과물 |
|---|---|
| 📁 **문서 관리** | 폴더·검색·정렬·카드 미리보기·복제·즐겨찾기·태그·메모를 갖춘 **문서 관리자** |
| ☁ **구글 드라이브** | 드라이브에서 **읽어오기**, 저장 구조 개편(**최신 1개 메인 / 나머지 Backup 폴더**), 정렬·백업 토글 |
| 📱 **모바일** | 터치 전용 **`mobile.html`** 신설(편집·맵·문서·드라이브·길게 누르기 메뉴·빠른 추가) |
| 🎨 **랜딩** | 접속마다 랜덤으로 뜨는 **소개 페이지 10종** + 분배기 |
| ✏️ **편집 UX** | 우클릭 순서 이동, 패널 폭 드래그, 다중 선택 일괄 편집, 선택 노드 앵커 확대/축소, 날짜·시간 삽입 단축키, 태그 라이브러리 |
| 🐛 **버그 수정** | 5라운드 집중 감사(데이터 손실·내보내기·Undo·정합·최적화), 노드 id 중복 자가 치유, 타이머 겹침 등 |
| 🔒 **보안** | 3축 병렬 감사 → 모바일 XSS 수정 + 클릭재킹 방어·부분 CSP·모바일 로그아웃 등 하드닝 |

---

## 1. 문서 관리자 — 여러 문서를 제대로 다루기

### v3.83.0 · 문서 관리자 (폴더·검색·정렬·카드·복제·즐겨찾기)
단순 목록이던 문서 화면을 **일반 브레인스토밍 툴 수준의 파일 매니저**로 확장했습니다.
- **폴더**로 정리: `전체 · ★즐겨찾기 · 미분류 · 내 폴더`
- **카드 미리보기**: 상위 가지·노드 수·수정일이 카드에 표시
- **검색·정렬**(최근/생성일/이름), **복제 ⧉**, **즐겨찾기 고정 ★**, **폴더 이동**, **JSON을 새 문서로 가져오기**
- 하위호환: 기존 문서를 자동으로 새 구조로 승격(데이터 보존)
<details><summary>기술 메모</summary>

- `docMeta.list` 항목에 `createdAt·pinned·folderId·nodeCount·branches` 추가 + `docMeta.folders[]`
- `normalizeDocItem`으로 옛 항목 안전 보정, `writeActiveDoc`가 저장 시 통계 갱신하며 기존 필드 보존
- 출시 전 **다차원 코드리뷰 + 적대적 검증**(React/저장/동작/UI/회귀 5축)으로 7건 수정
</details>

### v3.85.0 · 문서 태그 · 메모
- 각 문서에 **태그(여러 개)** 와 **메모**를 달 수 있음(카드의 `🏷`)
- 카드에 `#태그` 표시, 클릭 시 빠른 검색, 검색이 **태그·메모까지** 매칭

---

## 2. 구글 드라이브 — 읽어오기 + 저장 구조 개편

### v3.84.0 · 문서 관리자에서 드라이브 읽어오기
워드·파워포인트의 "열기"처럼 **드라이브에 저장한 마인드맵을 카드로 골라 새 문서로 가져오기**.
- 클릭=미리보기(노드 수·중심·본문 일부), 더블클릭=열기
- `📂 열기`(가져와 편집) / `📥 가져오기`(새 문서로만) / 검색 / `가져옴` 배지
- **읽기 전용** — 현재 문서를 덮지 않음
<details><summary>출시 전 리뷰에서 잡은 HIGH 버그</summary>

가져와서 "열기"할 때 IndexedDB 쓰기를 기다리지 않고 곧바로 다시 읽어서, 드물게 **빈 문서로 전환→자동저장이 덮어써 소실**될 수 있던 레이스 → 쓰기 `await` + 메모리에서 바로 전환으로 수정.
</details>

### v3.85.0 · 드라이브 목록 정렬
드라이브 뷰에 **최신순·오래된순·이름순·크기순** 정렬 추가.

### v3.86.0 ~ v3.86.2 · 저장 구조 개편 (준호님과 설계 확정 후 진행)
> **핵심 변경**: 저장 폴더엔 **가장 최신본 1개만**, 이전 버전은 그 안의 **`Backup` 하위 폴더**로 자동 이동. 보관 개수(기본 5)를 넘으면 오래된 백업부터 정리. 불러오기는 **메인+Backup을 합쳐 최신순**으로 표시.

- `v3.86.0`: 저장 구조 개편 + 불러오기 병합(백업 배지)
- `v3.86.1`: **버그 수정** — 다른 브라우저/세션 코드명(마우이·모나코 등) 파일이 안 옮겨지던 문제. `drive.file` 범위라 폴더엔 앱 파일만 있으므로 접두어 필터 제거, 정리는 "같은 이름 계열별로" 보관해 다른 날짜 이력 보존
- `v3.86.2`: 드라이브 목록 **기본은 최신본만**, `🗂 백업 보기` 토글로 이전 버전까지
<details><summary>출시 전 데이터-안전 리뷰</summary>

버전 단조성·충돌 감지·이동/정리·무관 파일 보호를 전수 검증. 시각 누락 시 `Date.parse`가 2000년으로 평가되던 잠재 정렬 결함 수정.
</details>

---

## 3. 집중 버그 감사 (v3.86.3 ~ v3.86.8)

준호님의 "버그 찾아서 고치기" 요청으로 **4개 병렬 리뷰 에이전트**(레이아웃·성능·상태·편집)를 돌려 확인된 버그를 **5라운드**로 나눠 수정했습니다. (오탐은 재검증으로 걸러냄)

| 라운드 | 버전 | 주요 수정 |
|---|---|---|
| — | v3.86.3 | 오버레이 뒤 단축키 누출, 비현재 문서 이름 불일치, 몰입 모드 흐려짐 등 4건 |
| 1 | v3.86.4 | 🔴 **시작 "새로 시작"이 기존 문서를 덮어쓰던 데이터 손실**, 이름변경↔자동저장 경합 |
| 2 | v3.86.5 | 🔴 접힘 상태 **SVG/PDF/JPG 내보내기 깨짐**, CSV 수식 주입 가드, 충돌검사 오판, 저장 중복 |
| 3 | v3.86.6 | 🔴 텍스트 편집 **Undo 시 맵·텍스트 어긋남**, Esc로 새 노드 취소 불가 |
| 4 | v3.86.7 | 빈 라벨 왕복 소실, 동명 형제 메타 뒤바뀜, 노드 폭 보존 |
| 5 | v3.86.8 | 상호작용마다의 O(n) 재순회 제거, 죽은 코드 정리, 병합 로직 통일 |

---

## 4. 첫 화면 — 랜딩 페이지

### v3.87.0 · 첫 방문자 랜딩 페이지
- `landing.html` 추가 — "밤의 캔버스" 소개 페이지
- 앱 사용 이력이 없는 브라우저만 랜딩으로 안내(`bb_visited`/`bb_drive_prefix`), `?app`으로 건너뛰기
- ⚠️ 통합 중 발견: **GitHub 웹 직접 업로드본이 AdGuard 확장 스크립트로 오염 + 구버전(v3.86.3)** 이었음 → 정리본으로 복구하며 배포

### v3.91.0 ~ v3.91.3 · 랜덤 랜딩 10종
- 디자인 10종(`landing01~10.html`): 밤의 캔버스·여백의 정원·열린 스튜디오·활판 포스터·텍스트 에디터·새벽 타임라인·간트 리듬·피어나는 정원·생각의 설계도·첫 한 줄
- `landing.html`이 **랜덤 분배기** — 접속마다 하나, `N/10 ↻` 버튼은 방금 본 것 제외하고 다시 뽑기
- `v3.91.1~3`: 주소창에 파일명 안 보이게 → **도메인만**(`history.replaceState`) — 랜딩·앱·모바일 전부

---

## 5. 모바일 전용 버전

### v3.88.0 · 📱 mobile.html 신설
데스크톱 앱은 터치 이벤트가 없어 폰에서 조작이 사실상 불가 → **터치 퍼스트 경량 앱**을 별도 파일로 신설.
- **편집(아웃라인)**: 탭=선택→재탭=편집, 접기, `⋯` 액션 시트(추가·이동·색·완료·삭제)
- **맵(보기)**: SVG 렌더, 한 손가락 이동·두 손가락 핀치줌
- **문서**: 목록·전환·새 문서·이름변경·삭제
- **☁ 드라이브**: 저장(데스크톱과 같은 규칙)·불러오기(메인+Backup 병합)
- 폰이면 자동 안내, `?desktop`으로 데스크톱 고정
<details><summary>기술 메모</summary>

프레임워크 없는 **바닐라 JS 단일 파일**(모바일 로딩 속도). 데스크톱과 **같은 IndexedDB·드라이브 규칙 공유**(문서가 안전하게 이어짐). 새 노드 id는 `m` 접두로 데스크톱(`n…`)과 충돌 회피.
</details>

### v3.89.0 · 길게 누르기 메뉴
노드를 **0.5초 꾹** 누르면 진동과 함께 메뉴 — 추가(같은/하위 레벨)·이름 바꾸기·이동·완료·색·삭제.

### v3.90.0 · 맵에서 바로 노드 추가
맵 탭에도 `＋` 버튼 + 하단 **빠른 입력 바**로 화면 전환 없이 이름 입력.

---

## 6. 편집 경험 개선

### v3.92.0 · 구글폼 피드백 링크
로그인 불필요·익명 폼을 **설정·about·모바일 메뉴** 세 곳에 연결.

### v3.92.1 · 헤더 타이머 겹침 수정
타이머가 `absolute`로 화면 정중앙에 고정돼 버튼과 겹치던 문제 → **flex 흐름**(빈 공간 중앙)으로 변경, 물리적으로 겹칠 수 없게.

### v3.93.0 · 피드백 3종 (준호님 요청)
- **우클릭 메뉴에서 순서 이동** — `🔼 위로 / 🔽 아래로`(메뉴 유지로 연속 이동)
- **패널 폭 드래그** — 좌/우 패널 경계를 끌어 조절(더블클릭=기본, 새로고침 후 유지)
- **다중 선택 일괄 편집** — Shift+클릭 후 배너에서 14색·할 일 한 번에

### v3.93.1 · 선택 노드 앵커 확대/축소
`+`/`−`·줌 버튼이 **선택한 노드를 기준점**으로 — 노드가 화면에서 있던 자리를 지킨 채 주변만 확대/축소.

### v3.94.0 · 태그 라이브러리
우측 패널에 **"사용한 태그" 목록** — 클릭으로 재사용(추가/제거 토글), `✎` 전체 이름 변경, `✕` 전체 삭제(Ctrl+Z 복구).

### v3.94.1 · 태그별 노드 수 배지
하단 태그 필터 바·태그 라이브러리의 각 태그에 **묶인 노드 수** 표시.

### v3.95.0 · 날짜/시간 삽입 단축키
`Ctrl+;` = 오늘 날짜, `Ctrl+Shift+;` = 현재 시간 (엑셀과 같은 키). 입력 중엔 커서 위치에, 아니면 노드 이름 끝에.

---

## 7. 데이터 무결성 · 보안

### v3.95.1 · 노드 id 중복 자가 치유 (준호님 제보)
**증상**: 노드가 연결선 없이 떠 있고, 지우면 관계없는 노드까지 같이 삭제.
**원인**: 두 노드가 **같은 내부 id**를 공유 → 렌더 key 충돌(위치 꼬임) + 삭제가 id로 동작해 둘 다 제거.
**수정**: `healDuplicateIds()`가 문서 로드·재파싱 시 중복 id를 자동 수리 + 삭제는 정확히 1개만. **기존 오염 문서도 다음 로드 때 자동 복구.**

### v3.95.2 ~ v3.95.3 · 전면 보안 재점검 (3축 병렬 감사)
XSS·공급망/CSP·인증/토큰/저장·프로토타입 오염을 **3개 에이전트 병렬**로 감사.

**실제 취약점 1건 — 수정 완료**:
- 🔴 **[MED] 모바일 맵 SVG 노드 id 미이스케이프 → 저장형 XSS** (악성 파일 가져오기 → 모바일 맵 열면 토큰·데이터 탈취 가능) → `esc()` 적용 (v3.95.2)

**하드닝 (v3.95.3)**:
- 클릭재킹 방어(JS 프레임버스팅), 부분 CSP(`object-src 'none'; base-uri 'self'`), 모바일 구글 연결 해제(revoke), 가져오기 정화 강화

**안전 확인**: 프로토타입 오염 없음(실증 테스트 통과), 토큰 유출·오픈 리다이렉트·시크릿 노출 없음, 공급망 SRI 4종 완비. `SECURITY.md` 갱신.

---

## 🗂 저장소 구성 (현재)

```
index.html          # 데스크톱 앱 본체 (현재 v3.95.3)
mobile.html         # 📱 모바일 전용 버전
landing.html        # 첫 방문 랜딩 랜덤 분배기
landing01~10.html   # 랜딩 디자인 10종
about.html          # 공지·개발자의 말
seahyun/            # 버전별 스냅샷 보관(brainstorm_v*.html)
README / CHANGELOG / SECURITY / CLAUDE.md   # 문서·이력·보안·작업지침
BrainBloom_UserGuide.html / _TechDoc.html   # 사용 설명서 / 기술 문서
```

## 🧭 작업 관례 (기억용)
- 호칭: **준호님**
- `.` 만 입력 → 진행/대기 작업 표로 정리
- 릴리스: 기능=마이너(x.Y.0) / 버그·미세조정=패치(x.y.Z), 매번 스냅샷·CHANGELOG·RECENT_CHANGES 갱신
- `lastWork`(IndexedDB)는 현재 문서 거울 — 항상 유지 / 드라이브 파일명 규칙은 동기화 파싱이 의존

## ⏸ 남은 큰 과제 (준호님 결정 대기)
1. **소스 비공개 + 난독화 배포 분리** — 외부 오픈 대비
2. **드라이브 문서별 분리 동기화** — 같은 날 여러 문서 편집 시 백업이 한 계열로 묶이는 구조 개선(파일명에 문서 ID 도입)
3. **GA 사용 통계** — 대시보드 가이드 또는 익명 사용 이벤트 보강

---

*이 기록은 2026-07-13 세션 기준입니다. 최신 상세는 항상 [CHANGELOG.md](CHANGELOG.md)를 참고하세요.*
