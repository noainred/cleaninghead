# 구글 캘린더 "바로 등록" 활성화 가이드 (관리자용)

v3.109.0부터 "📅 캘린더에 추가" → **캘린더에 바로 등록**은 앱이 구글 Calendar API로 일정을 직접 넣습니다.
이 기능이 실제로 동작하려면 아래 GCP(구글 클라우드) 설정이 **한 번** 필요합니다.
설정 전에도 같은 창의 **"제미나이로 등록"** 은 바로 사용할 수 있습니다(권한 불필요).

앱의 OAuth 클라이언트 ID: `775516377969-...apps.googleusercontent.com`
(이 ID가 속한 GCP 프로젝트에서 아래를 진행하세요.)

## 1. Calendar API 활성화
1. https://console.cloud.google.com 접속 → 위 클라이언트 ID가 속한 프로젝트 선택
2. **API 및 서비스 → 라이브러리** → "Google Calendar API" 검색 → **사용 설정**

## 2. OAuth 동의 화면에 권한(scope) 추가
1. **API 및 서비스 → OAuth 동의 화면 → 데이터 액세스(범위)**
2. **범위 추가** → `https://www.googleapis.com/auth/calendar.events` 추가 → 저장
   - 이 scope는 구글의 "민감한 범위"라, 공개 앱으로 검증받기 전에는 로그인 시
     "확인되지 않은 앱" 경고가 뜰 수 있습니다.

## 3. (검증 전) 테스트 사용자 등록 — 개인/소수 사용 시 권장
- OAuth 동의 화면이 **테스트** 상태라면, **대상(테스트 사용자)** 에 캘린더를 쓸
  구글 계정(준호님 계정 등)을 추가하세요. 등록된 계정은 경고 없이 바로 사용됩니다.
- 불특정 다수 공개 사용까지 필요하면 구글 **앱 검증**을 받아야 합니다(수일~수주 소요).

## 4. 확인
- 설정 후 앱에서 "📅 캘린더에 추가 → 일정 해석하기 → 캘린더에 바로 등록"
- 처음 1회 **캘린더 권한 동의 팝업**이 뜹니다(기존 드라이브 로그인과 별개, 증분 동의).
  동의하면 해석된 일정이 내 기본 캘린더에 등록됩니다.

## 동작 안 할 때
- "바로 등록"을 눌렀는데 자동으로 제미나이 안내가 뜨면 → 위 1~3 중 누락이 있는 것입니다.
- 등록은 되는데 일부만 실패 → 그 줄의 날짜/시간 형식을 확인하거나 제미나이로 재시도.

## 롤백
- 이 기능은 기존 동작에 영향이 없습니다. GCP 설정을 안 하면 "제미나이로 등록"만 쓰면 됩니다.
- 코드 롤백이 필요하면 해당 릴리스(PR) revert 후 재배포.
